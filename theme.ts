import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#22577A",
    },
    secondary: {
      main: "#38A3A5",
      light: "#6EF3D6",
      dark: "#38A3A5",
    },
  },
});

export default theme;
